{
  "compilerOptions": {
    "baseUrl": "./src",    // Base directory for resolving non-relative module names
    "paths": {
      "@/*": ["./*"]       // Maps '@' to the 'src' directory
    }
  },
  "include": ["src/**/*"]   // Specifies the directories that should be included in the compilation
}
